jQuery(document).ready(function($){let frame;$('.fms-upload-image').on('click',function(e){e.preventDefault();const button=$(this);if(frame){frame.open();return;}frame=wp.media({title:'Choisir une image',button:{text:'Utiliser cette image'},multiple:false});frame.on('select',function(){const attachment=frame.state().get('selection').first().toJSON();button.prev('.fms-image-id').val(attachment.id);button.siblings('.fms-image-preview').html('<img src="'+attachment.url+'" style="max-width:150px;height:auto;" />');});frame.open();});$('.fms-remove-image').on('click',function(e){e.preventDefault();$(this).siblings('.fms-image-id').val('');$(this).siblings('.fms-image-preview').html('');});});